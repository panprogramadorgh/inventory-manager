file(STRINGS "@CMAKE_CURRENT_BINARY_DIR@/install_manifest.txt" files_to_remove)
foreach(file ${files_to_remove})
  file(REMOVE ${file})
  if(EXISTS ${file})
    message(WARNING "Failed to remove ${file}")
  else()
    message(STATUS "Removed ${file}")
  endif()
endforeach()
